# 2025前端面试题合集

> 本文档整理自掘金【2025前端面试题】系列文章，涵盖Vue3、React、JavaScript、TypeScript等核心技术栈的面试考察点。

---

## 目录

- [TypeScript 理论篇](#typescript-理论篇)
- [TypeScript 实战篇](#typescript-实战篇)
- [Vue3 进阶篇](#vue3-进阶篇)
- [React 基础篇](#react-基础篇)
- [React 进阶篇](#react-进阶篇)
- [React 高阶篇](#react-高阶篇)
- [JavaScript 中级篇](#javascript-中级篇)
- [JavaScript 基础篇](#javascript-基础篇)

---

## TypeScript 理论篇

> 来源：[2025前端面试题-TS理论篇](https://juejin.cn/post/7530179511728930856)

### 一、核心类型系统

#### 1. 基础类型

**原始类型**
```typescript
let name: string = 'Tom';
let age: number = 30;
let isActive: boolean = true;
let nothing: null = null;
let notDefined: undefined = undefined;
let uniqueKey: symbol = Symbol('id');
```

**数组与元组**
```typescript
// 数组
let numbers: number[] = [1, 2, 3];
let strings: Array<string> = ['a', 'b', 'c'];

// 元组（固定长度和类型）
let person: [string, number] = ['Tom', 10];
person[0] = 'Jerry';     // ✅
person[1] = '20';       //  ❌ 类型错误
```

**特殊类型对比**

| 类型 | 特点 | 使用场景 |
| --- | --- | --- |
| `any` | 禁用类型检查，与原生 JS 行为一致 | 迁移 JS 项目、快速原型设计 |
| `unknown` | 类型安全的 `any`，使用前必须断言或类型检查 | 接收第三方库数据、API 响应处理 |
| `never` | 表示永远不会发生的值 | 函数抛出异常、穷尽检查 |
| `void` | 函数无返回值（与 `undefined`兼容但不等价） | 函数副作用操作 |

```typescript
// any vs unknown
let anyValue: any = 'hello';
anyValue.toFixed(2); // ❌ 运行时错误

let unknownValue: unknown = 'hello';
if (typeof unknownValue === 'string') {
    unknownValue.toUpperCase();  // ✅ 优先类型检查
}

// never 使用示例
function throwError(message: string) {
    throw new Error(message);
}

// void 函数
function logMessage(msg: string) {
    console.log(msg);
    // 等价于 undefined，但 null 会报错
}
```

#### 2. 类型推断与断言

**类型推断**
```typescript
// 自动推断变量类型
let x = 10;        // 推断为 number
// 函数返回类型推断
function add(a: number, b: number) {
    return a + b; // 推断返回 number
}
```

**类型断言**
```typescript
// as 断言（推荐）
const element = document.getElementById('root') as HTMLDivElement;

// 非空断言（!）
const user!: { name: string }; // 强制认为非空（危险！）
const el = document.querySelector('.btn')!;

// const 断言（值不可变）
let arr = [1, 2] as const; // 类型变为 readonly [1, 2]
arr.push(3); // ❌ 编译错误
```

#### 3. 类型兼容性

**结构化类型（鸭子类型）**
```typescript
interface Person {
    name: string;
    age: number;
}

let tom = { name: 'Tom', age: 20, gender: 'M' };
const bob: Person = tom; // ✅ 额外属性不破坏兼容性

// 空对象兼容（特殊情况）
const empty = {};
const p: Person = empty; // ❌ 缺少必要属性
```

**函数类型兼容**
```typescript
type Handler = (data: string) => void;

// ✅ 参数兼容：目标类型参数 < 源类型参数
const handleEvent: Handler = (data: string | number) => console.log(data);

// ❌ 错误示例：源类型参数 > 目标类型参数
const handler: (data: string | number) => void = (data: string) => console.log(data);
```

**逆变与协变**

开启`strictFunctionTypes`后：
- **参数类型**：逆变（接收更具体的类型）
- **返回类型**：协变（返回更宽泛的类型）

```typescript
// 逆变演示（参数兼容）
type AnimalHandler = (animal: Animal) => void;
type DogHandler = (dog: Dog) => void;

let animalFunc: AnimalHandler = (a: Animal) => {};
let dogFunc: DogHandler = (d: Dog) => {};

dogFunc = animalFunc; // ✅ 安全（目标函数能处理更多类型）
animalFunc = dogFunc; // ❌ 危险（关闭strictFunctionTypes时允许）

// 协变示例（返回值兼容）
type AnimalCreator = () => Animal;
type DogCreator = () => Dog;

let createAnimal: AnimalCreator = () => new Animal();
let createDog: DogCreator = () => new Dog();

createAnimal = createDog; // ✅ 安全（返回值更具体）
createDog = createAnimal; // ❌ 错误（可能返回非Dog对象）
```

### 二、高级类型编程

#### 1. 泛型

**泛型函数/类/接口**
```typescript
// 泛型函数
function identity<T>(arg: T): T { return arg; }
const num = identity<number>(42);  // 显式指定
const str = identity("hello");     // 自动推断为 string

// 泛型接口
interface KeyValuePair<K, V> {
    key: K;
    value: V;
}

// 泛型类
class DataWrapper<T> {
    constructor(public value: T) {}
    getValue(): T { return this.value; }
}
```

**泛型约束与默认类型**
```typescript
// 约束类型必须具有length属性
interface LengthWise { length: number; }
function logLength<T extends LengthWise>(obj: T): void {
    console.log(obj.length);
}

// 默认类型参数
interface Pagination<T = string> {
    data: T[];
    page: number;
}
```

**类型参数推导（infer）**
```typescript
// 获取函数返回类型
type ReturnType<T> = T extends (...args: any) => infer R ? R : never;
// 获取数组元素类型
type ArrayElement<T> = T extends (infer U)[] ? U : never;
```

#### 2. 工具类型与类型编程

**内置工具类型**

| 工具类型 | 作用 | 等价写法 |
| --- | --- | --- |
| `Partial<T>` | 所有属性变为可选 | `{ [P in keyof T]?: T[P] }` |
| `Required<T>` | 所有属性变为必填 | `{ [P in keyof T]-?: T[P] }` |
| `Readonly<T>` | 所有属性变为只读 | `{ readonly [P in keyof T]: T[P] }` |
| `Pick<T, K>` | 选取T中指定属性K | `{ [P in K]: T[P] }` |
| `Omit<T, K>` | 排除T中指定属性K | `Pick<T, Exclude<keyof T, K>>` |
| `Record<K, T>` | 创建键为K类型，值为T类型的对象 | `{ [P in K]: T }` |

**映射类型**
```typescript
// 将所有属性转为getter函数
type Getters<T> = {
    [K in keyof T as `get${Capitalize<string & K>}`]: () => T[K]
};

interface Person {
    name: string;
    age: number;
}

type PersonGetters = Getters<Person>;
// 等价于
{
    getName: () => string;
    getAge: () => number;
}
```

**条件类型**
```typescript
// 处理Promise返回值
type AsyncResult<T> = T extends Promise<infer U> ? U : T;
// 深度可选
type DeepPartial<T> = T extends object ? { [P in keyof T]?: DeepPartial<T[P]> } : T
```

**模板字面量类型**
```typescript
type HttpMethod = 'GET' | 'POST' | 'PUT';
type ApiRoute = `/api/${string}`;

type ApiEndpoint = `${HttpMethod} ${ApiRoute}`;
const endpoint: ApiEndpoint = 'GET /api/users';
```

#### 3. 类型守卫与类型收窄

**内置守卫**
```typescript
// typeof
function padLeft(value: string, padding: string | number) {
    if (typeof padding === 'number') {
        return ' '.repeat(padding) + value;
    }
    return padding + value;
}

// instanceof
class Animal {}
class Bird extends Animal { fly() {} }

// in 操作符
interface Fish { swim(): void }
interface Dog { run(): void }
function action(pet: Fish | Dog) {
    if ('swim' in pet) {
        pet.swim(); // pet: Fish
    }
}
```

**自定义类型守卫**
```typescript
// 类型谓词语法：args is Type
function isString(value: unknown): value is string {
    return typeof value === 'string';
}

function process(input: string | number) {
    if(isString(input)) {
        input.toUpperCase(); // input: string
    }
}
```

**可辨识联合**
```typescript
type Shape =
    | { kind: 'circle'; radius: number }
    | { kind: 'square'; side: number };
function getArea(shape: Shape) {
    switch (shape.kind) {
        case 'circle':
            return Math.PI * shape.radius ** 2;
        case 'square':
            return shape.side ** 2;
    }
}
```

### 三、面向对象与设计

#### 1. 类与继承

**访问修饰符**

| 修饰符 | 类内部 | 子类 | 实例 | 不可重新赋值 |
| --- | --- | --- | --- | --- |
| `public` | ✓ | ✓ | ✓ | ✗ |
| `protected` | ✓ | ✓ | ✗ | ✗ |
| `private` | ✓ | ✗ | ✗ | ✗ |
| `readonly` | ✓ | ✓ | ✓ | ✓ |

**抽象类与抽象方法**
```typescript
abstract class Shape {
    abstract getArea(): number;  // 抽象方法无实现

    printArea() {
        console.log(`Area: ${this.getArea()}`);
    }
}

class Circle extends Shape {
    constructor(private radius: number) { super(); }

    getArea() {
        return Math.PI * this.radius ** 2;
    }
}
```

#### 2. 接口 vs 类型别名

**声明合并**
```typescript
// 接口自动合并
interface User {
    name: string;
}

interface User {
    age: number;
}

const user: User = {
    name: 'Tom',
    age: 30,
};

// 类型别名不可重复
type Person = { name: string };
type Person = { age: number }; // ❌ 重复标识符
```

**适用场景对比**

| 特性 | 接口 | 类型别名 |
| --- | --- | --- |
| 声明合并 | ✓ | ✗ |
| 扩展方式 | `extends` | `&`(交叉类型) |
| 实现类 | ✓ (`implements`) | ✗ |
| 联合/元组等复杂类型 | ✗ | ✓ (`type T = A`) |
| 映射类型 | ✗ | ✓ |

**最佳实践**
- 定义对象形状优先用接口
- 复杂类型组合（联合、元组）用类型别名
- 库类型定义优先用接口（支持使用者扩展）

#### 3. 装饰器

```typescript
// 类装饰器
@sealed
class Person {}

function sealed(constructor: Function) {
    Object.seal(constructor);
    Object.seal(constructor.prototype);
}

// 方法装饰器
class MathOps {
    @log
    add(a: number, b: number) {
        return a + b;
    }
}

function log(target: any, key: string, desc: PropertyDescriptor) {
    const original = desc.value;
    desc.value = function (...args: any[]) {
        console.log(`Calling ${key} with`, args);
        return original.apply(this, args);
    }
}
```

---

## TypeScript 实战篇

> 来源：[2025前端面试题-TS实战篇](https://juejin.cn/post/7533826195352829988)

### 四、模块与工程化

#### 1. 模块系统

**ES Module vs CommonJS 类型声明**
```typescript
// ES Module（推荐）
import { func } from './module';
export const value = 42;

// CommonJS（需类型声明）
const _ = require('lodash');
import _ from 'lodash';      // 需安装 @type/lodash

// 类型声明示例（module.d.ts）
declare module 'my-legacy-lib' {
    export function oldFunc(): void();
}
```

**import type 类型隔离**
```typescript
// 仅导入类型（编译后移出）
import type { User } from './types';

// 混合导入
import { fetchUser, type User } from './api';
```

#### 2. 配置与编译

**tsconfig.json 核心配置**

| 配置项 | 作用 | 推荐值 |
| --- | --- | --- |
| `target` | 编译目标 JS 版本 | `ES2020`/ `ESNext` |
| `module` | 模块系统 | `ESNext`(Web) / `CommonJS`(Node) |
| `strict` | 启用所有严格检查 | `true`(新项目必开) |
| `declaration` | 生成 `.d.ts`类型声明文件 | `true`(库开发必备) |

**核心配置**
```json
{
  "compilerOptions": {
    "target": "ES2020",
    "module": "ESNext",
    "moduleResolution": "node",
    "strict": true,
    "declaration": true,
    "sourceMap": true,
    "outDir": "dist",
    "esModuleInterop": true,
    "skipLibCheck": true
  },
  "include": ["src/**/*.ts"],
  "exclude": ["node_modules", "**/*.test.ts"]
}
```

### 五、工程实践与高级特性

#### 1. 声明文件（`.d.ts`）

**为第三方库编写类型声明**
```typescript
// my-lib.d.ts
declare module 'untyped-lib' {
    export function calculate(a: number, b: number): number;
    export const version: string;
}
```

**模块扩充**
```typescript
// 扩展express Request对象
declare module 'express' {
    interface Request {
        user?: { id: string; role: string };
    }
}
```

**全局类型声明**
```typescript
declare global {
    interface Window {
        __APP_CONFIG__: {
            apiBase: string;
            debug: boolean;
        }
    }
}
```

#### 2. 异步与并发

**Promise<T> 类型**
```typescript
function fetchData(): Promise<{ data: string }> {
    return fetch('/api').then(res => res.json());
}

async function getUser(id: string): Promise<User> {
    try {
        const response = await fetch(`/user/${id}`);
        if (!response.ok) throw new Error('Not Found');
        return response.json();
    } catch (error) {
        if (error instanceof Error) {
            console.error(error.message);
        }
        throw error;
    }
}
```

#### 3. 实用技巧

**keyof 与索引访问类型**
```typescript
interface User {
    id: number;
    name: string;
    email: string;
}

type UserKeys = keyof User; // 'id' | 'name' | 'email'

function getProperty<T, K extends keyof T>(obj: T, key: K): T[K] {
    return obj[key];
}
```

**this 类型与链式调用**
```typescript
class Calculator {
    value: number;

    constructor(value = 0) {
        this.value = value;
    }

    add(n: number): this {
        this.value += n;
        return this;
    }

    multiply(n: number): this {
        this.value *= n;
        return this;
    }
}

const result = new Calculator(10)
    .add(5)
    .multiply(2)
    .value; // 30
```

**satisfies 操作符（TS 4.9+）**
```typescript
const colors = {
    red: '#FF0000',
    green: '#00FF00',
    blue: '#0000FF'
} satisfies Record<string, string>;
```

### 六、框架集成

#### 1. React + TS

**组件类型定义**
```typescript
interface Props {
  title: string;
  disabled?: boolean;
  children: React.ReactNode;
}

const Button: React.FC<Props> = ({ title, disabled = false, children }) => {
  return (
    <button disabled={disabled}>
        {title} - {children}
    </button>
  )
}
```

**Hooks类型**
```typescript
const [count, setCount] = useState<number>(0);
const inputRef = useRef<HTMLInputElement>(null);
```

#### 2. Vue + TS

**defineComponent**
```typescript
import { defineComponent, ref, reactive } from 'vue';

interface User {
  id: number;
  name: string;
}

export default defineComponent({
  props: {
    initialCount: { type: Number, default: 0 }
  },
  setup(props) {
    const count = ref<number>(props.initialCount);
    const user = reactive<User>({ id: 1, name: 'Alice' });
    return { count, user };
  }
})
```

---

## Vue3 基础篇

> 来源：[2025前端面试题-Vue3基础篇](https://juejin.cn/post/7503111373468893193)

### 一、生命周期

#### 1. 核心变化

**组合式API的引入**
- `setup()`替代`beforeCreate`和`created`。所有组合式API逻辑在此函数中初始化，替代Vue2的`data`、`method`等选项式配置
- 钩子函数前缀`on`。生命周期函数需要从Vue显示导入，如`onMounted`，且只能在`setup()`或`<script setup>`中使用

**钩子函数重命名**
- `beforeDestroy` → `onBeforeUnmount`
- `destroyed` → `onUnmounted`

**新增钩子**
- `onServerPrefetch`：服务端渲染（SSR）期间异步获取数据
- 调试钩子：`onRenderTracked`（跟踪响应式依赖）、`onRenderTriggered`（响应式变更触发）

#### 2. 生命周期阶段详解

**初始化阶段**
- `setup()`：替代`beforeCreated`和`created`，初始化响应式数据、方法等。注意：无法访问`this`
- 选项式API兼容：`beforeCreated`和`created`仍可用，但���免与`setup()`混用

**挂载阶段**
- `onBeforeMount`：组件挂载到DOM前调用，此时虚拟DOM已生成但未渲染
- `onMounted`：组件挂载完成，可操作DOM或发起网络请求

**更新阶段**
- `onBeforeUpdate`：数据变化导致DOM更新前触发，适合获取更新前的DOM状态
- `onUpdated`：DOM更新执行后，避免在此修改状态，可能导致无限循环

**卸载阶段**
- `onBeforeUnmount`：组件卸载前调用，清理定时器，取消网络请求，移除事件监听
- `onUnmounted`：组件卸载后触发，此时子组件已全部卸载

**其他钩子**
- `onErrorCaptured`：捕获子孙组件错误，可返回`false`阻止冒泡
- `onActivated`/`onDeactivated`：`<KeepAlive>`缓存组件激活/停用时调用

#### 3. 高频面试题

**Q1: 父子组件生命周期执行顺序？**
- 挂载阶段：父`onBeforeMount` → 子`onBeforeMount` → 子`onMounted` → 父`onMounted`
- 更新阶段：父`onBeforeUpdate` → 子`onBeforeUpdate` → 子`onUpdated` → 父`onUpdated`
- 卸载阶段：父`onBeforeUnmount` → 子`onBeforeUnmount` → 子`onUnmounted` → 父`onUnmounted`

**Q2: 在setup()中如何访问this？**
- `setup()`中没有`this`，响应式数据通过`ref`/`reactive`定义，方法直接声明

**Q3: 异步请求放在哪个钩子？**
- 客户端渲染（CSR）：`onMounted`（确保DOM可用）
- 服务端渲染（SSR）：`onServerPrefetch`

### 二、组件通信

#### 1. Props/Emits（父子通信）

**props（父→子）**
```vue
<!-- 父组件 -->
<Child :title="data" />
<!-- 子组件 -->
<script setup>
defineProps(['title'])
</script>
```

- 类型校验：`defineProps({ title: { type: String, required: true } })`
- 单项数据流：子组件不能直接修改props（需要通过emit通知父组件）

**emits（子→父）**
```vue
<!-- 子组件 -->
<button @click="$emit('update', value)"></button>
<!-- 父组件 -->
<Child @update="handleUpdate" />
```

- Vue3特性：`defineEmits(['update'])`显式声明事件

#### 2. v-model双向绑定

**单值绑定**
```vue
<!-- 父组件 -->
<Child v-model="message" />
<!-- 子组件 -->
<input :value="modelValue" @input="$emit('update:modelValue', $event.target.value)">
<script setup>
defineProps(["modelValue"])
defineEmits(["update:modelValue"])
</script>
```

**多值绑定**
```vue
<Child v-model:title="title" v-model:content="content" />
```

#### 3. ref/expose（父访问子组件）

**模板引用**
```vue
<!-- 父组件 -->
<Child ref="childRef" />
<script setup>
const childRef = ref(null);
// 访问子组件暴露的属性/方法
childRef.value.childMethod();
</script>

<!-- 子组件 -->
<script setup>
const childMethod = () => {};
defineExpose({ childMethod })
</script>
```

#### 4. provide/inject（跨层级通信）

```javascript
// 祖先组件
import { provide } from 'vue';
provide('theme', 'dark');

// 后代组件
import { inject } from 'vue';
const theme = inject('theme', 'light') // 第二个参数为默认值

// 提供响应式数据
const count = ref(0);
provide('count', count);
```

#### 5. 事件总线（Event Bus）

Vue3官方废弃`$on`，推荐第三方库（如mitt）

```javascript
// eventBus.js
import mitt from 'mitt';
export const emitter = mitt();

// 组件A（发布事件）
emitter.emit('refresh', data);

// 组件B（订阅事件）
emitter.on('refresh', (data) => { /* ... */ })

// 组件卸载时取消订阅
onUnmounted(() => emitter.off('refresh'))
```

#### 6. 状态管理（Pinia）

```javascript
// store/counter.js
export const useCounterStore = defineStore('counter', {
   state: () => ({ count: 0 }),
   actions: {
       increment() { this.count++; }
   }
})

// 组件中使用
const store = useCounterStore();
store.increment();
```

#### 7. 高频面试题

**Q1: 父子组件通信方式有哪些？**
- 父→子：props、`$attrs`、ref
- 子→父：emits、v-model
- 双向：v-model、状态管理

**Q2: provide/inject能否替代Vuex？**
- 适用场景：跨层级但关系明确的组件
- 局限性：不适合全局状态管理，无法跟踪状态变化历史

**Q3: Vue3为什么移除事件总线？**
- 设计理念：避免全局事件导致代码维护困难
- 代替方案：使用Pinia管理状态或mitt库实现事件总线

**Q4: 如何实现兄弟组件通信？**
- 通过共同父组件中转（props/emits）
- 事件总线（mitt）
- 状态管理（Pinia）

### 三、修饰符

#### 1. 事件修饰符

| 修饰符 | 作用 |
| --- | --- |
| `.stop` | 阻止事件冒泡 |
| `.prevent` | 阻止默认行为 |
| `.capture` | 使用捕获模式 |
| `.self` | 仅当事件从元素自身触发时执行 |
| `.once` | 事件只触发一次 |
| `.passive` | 提升滚动性能，不阻止默认行为 |

```vue
<button @click.stop="handleClick">点击</button>
<form @submit.prevent="onSubmit"></form>
<div @click.self="handleSelf">仅自身点击有效</div>
```

#### 2. v-model修饰符

| 修饰符 | 作用 |
| --- | --- |
| `.lazy` | 输入框失焦后更新数据（input→change事件） |
| `.number` | 将输入值转为数值类型 |
| `.trim` | 自动去除首位空格 |

```vue
<input v-model.lazy="message" />
<input v-model.number="age" type="number" />
<input v-model.trim="username" />
```

#### 3. 键盘修饰符

```vue
<input @keyup.enter="submit" />
<button @click.ctrl="handleCtrlClick">需按住 Ctrl 点击</button>
<button @click.ctrl.exact="onlyCtrl">仅 Ctrl 按下时触发</button>
```

#### 4. Vue3修饰符新特性

**`.sync`修饰符的替代**
- Vue2：通过`.sync`实现父子组件双向绑定
- Vue3：改用`v-model:propName` + `emit('update:propName')`

**`.native`修饰符移除**
- Vue2：组件上绑定原生事件需用`.native`
- Vue3：默认不绑定到根元素，需通过emits声明或手动绑定

#### 5. 高频面试题

**Q1: v-model的`.lazy`和`.sync`有何区别？**
- `.lazy`：延迟数据同步（input→change事件）
- `.sync`：Vue2中用于父子组件双向绑定，Vue3改用`v-model:prop`

**Q2: 如何阻止事件冒泡和默认行为？**
- 链式调用：`@click.stop.prevent`（顺序不影响效果）

### 四、指令

#### 1. 核心内置指令

**数据绑定**
- `v-bind`：动态绑定属性（缩写 `:`）
- `v-model`：表单双向绑定

**条件渲染**
- `v-if`/`v-else-if`/`v-else`：动态添加/移除DOM元素
- `v-show`：通过CSS切换显示（`display: none`）

**列表渲染**
```vue
<li v-for="(item, index) in items" :key="item.id">{{ index }}: {{ item.name }}</li>
```

- 必须指定key：优化虚拟DOM复用，避免渲染错误
- 避免与v-if共用：优先级问题（Vue2中v-for优先，Vue3中v-if优先）

**其他指令**
| 指令 | 作用 |
| --- | --- |
| `v-html` | 渲染原始HTML（警惕XSS攻击） |
| `v-text` | 替代`{{}}`插值 |
| `v-pre` | 跳过编译，保留原始内容 |
| `v-cloak` | 隐藏未编译的模板（配合CSS使用） |
| `v-once` | 仅渲染一次，后续数据变化不更新 |

#### 2. 自定义指令

**全局注册**
```javascript
app.directive('focus', {
  mounted(el) { el.focus(); }
});
```

**局部注册**
```vue
<script setup>
const vFocus = { mounted: (el) => el.focus() };
</script>
```

**指令生命周期钩子**
| 钩子 | 说明 |
| --- | --- |
| `created` | 元素属性初始化前 |
| `beforeMount` | 元素插入DOM前（Vue2的bind） |
| `mounted` | 元素插入DOM后（Vue2的inserted） |
| `beforeUpdate` | 组件更新前 |
| `updated` | 组件更新后 |
| `beforeUnmount` | 组件卸载前（Vue2的unbind） |
| `unmounted` | 组件卸载后 |

#### 3. 高频面试题

**Q1: v-if和v-show的区别？**
- `v-if`：条件为假时销毁DOM，适合不频繁切换场景
- `v-show`：始终保留DOM，通过CSS切换显示，适合频繁切换

**Q2: 为什么v-for需要key？**
- 虚拟DOM优化：key帮助Vue识别节点身份，避免错误复用元素，提升更新效率

### 五、ref和reactive的区别

#### 1. 核心区别

| 特性 | ref | reactive |
| --- | --- | --- |
| 基本类型 | 支持 | 不支持 |
| 访问方式 | 需要`.value` | 直接访问 |
| 解构响应性 | 需用`toRefs()` | 会丢失响应性 |
| 底层实现 | 封装`{value}`对象 | Proxy代理 |

#### 2. 高频面试题

**Q1: 为什么ref需要.value？**
- 设计目的：统一处理基本类型和对象类型
- 基本类型无法通过Proxy代理，ref通过封装对象（`{value: ...}`）实现响应式

**Q2: 如何选择ref和reactive？**
- **优先ref**：管理基本类型数据、需要明确的数据引用
- **优先reactive**：管理复杂对象/数组、需要深层嵌套的响应式数据

**Q3: 如何解构reactive对象且保持响应性？**
```javascript
const state = reactive({ count: 0, name: 'Vue' });
const { count, name } = toRefs(state); // 保持响应性
count.value++; // 生效
```

**Q4: reactive的局限性是什么？**
- 无法直接替换整个对象
```javascript
let obj = reactive({ a: 1 });
obj = { a: 2 }; // 响应式丢失！

// 解决方案
Object.assign(obj, { a: 2 }); // 保持响应性
```

### 六、slot插槽

#### 1. 核心概念

**默认插槽**
```vue
<!-- 父组件 -->
<Child>默认内容</Child>

<!-- 子组件 -->
<template>
  <slot>Fallback Content</slot>
</template>
```

**具名插槽**
```vue
<Child>
  <template #header>头部内容</template>
  <template #default>默认内容</template>
  <template #footer>底部内容</template>
</Child>

<!-- 子组件 -->
<template>
  <slot name="header"></slot>
  <slot></slot>
  <slot name="footer"></slot>
</template>
```

**作用域插槽**
```vue
<!-- 子组件 -->
<template>
  <slot :user="user" :data="data"></slot>
</template>

<!-- 父组件 -->
<Child>
  <template #default="slotProps">
    {{ slotProps.user.name }}
  </template>
</Child>
```

#### 2. Vue3插槽新特性

- 废弃`slot`属性：改用`v-slot`指令
- 废弃`slot-scope`：统一使用`v-slot`或`#`语法
- 支持动态插槽名：`<template #[dynamicSlotName]>`

#### 3. 高频面试题

**Q1: 作用域插槽的作用是什么？**
- 数据反向传递：允许子组件向父组件传递数据，实现内容渲染逻辑的定制

### 七、watch、watchEffect、computed的区别

#### 1. 核心区别

| 特性 | computed | watch | watchEffect |
| --- | --- | --- | --- |
| 用途 | 派生数据 | 监听特定数据变化 | 自动跟踪依赖变化 |
| 缓存 | 有缓存，惰性计算 | 无缓存 | 无缓存 |
| 返回值 | 返回计算结果 | 返回停止函数 | 返回停止函数 |
| 旧值 | 不支持 | 支持新旧值对比 | 不支持 |

#### 2. 高频面试题

**Q1: 三者核心区别是什么？**
- `computed`：派生数据，有缓存，惰性计算
- `watch`：显式监听数据源，支持新旧值对比和异步
- `watchEffect`：自动收集依赖，立即执行，无旧值

**Q2: watch的immediate和deep选项的作用？**
- `immediate: true`：立即执行回调（初始值触发）
- `deep: true`：深度监听对象/数组内部变化

**Q3: 如何停止watch或watchEffect的监听？**
```javascript
const stop = watch(data, callback);
stop(); // 停止监听
```

---

## Vue3 进阶篇

> 来源：[2025前端面试题-Vue3进阶篇](https://juejin.cn/post/7511568225987051555)

### 一、keep-alive

#### 1. 核心作用和使用场景

**作用**
- 缓存组件实例：避免重复销毁和创建，保留组件状态
- 提升性能：适用于需要频繁切换但状态需保留的组件

**使用方式**
```vue
<template>
  <keep-alive :include="['ComponentA', 'ComponentB']" :max="5">
    <component :is="currentComponent"></component>
  </keep-alive>
</template>
```

#### 2. 生命周期钩子变化

新增钩子（仅在被缓存的组件中触发）：
- `onActivated`：组件被激活（插入DOM）时触发
- `onDeactivated`：组件被停用（移除DOM）时触发

**执行顺序**
- 首次加载：`onCreate->onMounted->onActivated`
- 切换离开：`onDeactivated`
- 再次进入：`onActivated`
- 彻底销毁：`onUnmounted`

#### 3. 关键配置属性

**include**
- 匹配组件名称（name选项），仅缓存匹配的组件
- 支持字符串、正则、数组

**exclude**
- 排除指定组件，优先级高于include

**max**
- 最大缓存实例数，超出时按LRU（最近最少使用）策略淘汰旧实例

### 二、异步组件

#### 1. 核心概念与使用方式

**定义异步组件**
```typescript
import { defineAsyncComponent } from 'vue';
const AsyncCom = defineAsyncComponent(() => import('./MyComponent.vue'));
```

**高级配置**
```typescript
const AsyncComp = defineAsyncComponent({
    loader: () => import('./MyComponent.vue'),
    loadingComponent: LoadingSpinner,  // 加载中组件
    errorComponent: ErrorDisplay,      // 错误组件
    delay: 200,                       // 延迟显示loading
    timeout: 5000                     // 超时时间
})
```

**Suspense组件**
```vue
<template>
  <Suspense>
    <template #default>
      <AsyncComp />
    </template>
    <template #fallback>
      <div>Loading...</div>
    </template>
  </Suspense>
</template>
```

### 三、Vue-Router 4.X

#### 1. 核心变化

```typescript
import { createRouter, createWebHistory } from 'vue-router';
const router = createRouter({
    history: createWebHistory(),
    routes: [...]
});
```

**组合式API支持**
- `useRouter()`：获取路由实例
- `useRoute()`：获取当前路由对象

#### 2. 路由配置

**动态路由**
```typescript
{ path: '/user/:id', component: User };
```

**嵌套路由**
```typescript
{
    path: '/parent',
    component: Parent,
    children: [
        { path: 'child', component: Child }
    ]
}
```

**导航守卫**
```typescript
// 全局守卫
router.beforeEach((to, from, next) => { ... });
router.afterEach((to, from) => { ... });

// 组件内守卫
import { onBeforeRouteLeave } from 'vue-router';
onBeforeRouteLeave((to, from, next) => {
    // 清理逻辑
    next();
});
```

### 四、状态管理

#### 1. Pinia 核心概念

**Options Store**
```typescript
import { defineStore } from 'pinia';
export const useCounterStore = defineStore('counter', {
    state: () => ({ count: 0 }),
    getters: {
        doubleCount: (state) => state.count * 2,
    },
    actions: {
        increment() {
            this.count++;
        },
    },
});
```

**Setup Store**
```typescript
export const useUserStore = defineStore('user', () => {
    const name = ref('Alice');
    const setName = (newName: string) => { name.value = newName; };
    return { name, setName };
})
```

**Pinia vs Vuex**
- Pinia无mutations，直接通过actions修改状态
- Pinia基于模块化设计（每个Store独立）
- 更好的TypeScript支持

### 五、性能优化

#### 1. Vue3核心优化机制

- 基于`Proxy`替代Vue2的`Object.defineProperty`
- 静态提升：将静态节点提升到渲染函数外
- 补丁标志：标记动态绑定的类型
- Block Tree优化：划分动态和静态区块

#### 2. 组件级优化策略

```vue
<!-- v-once：静态内容只渲染一次 -->
<div v-once>永不更新的内容</div>

<!-- v-memo：依赖不变时跳过更新 -->
<div v-memo="[value]">{{ value }}</div>

<!-- v-show：高频切换 -->
<!-- v-if：低频切换 -->
```

**状态管理优化**
```typescript
// shallowRef/shalloReactive：非深度响应式数据
const largeObj = shallowRef({ ... })

// markRaw跳过响应式转换
const staticData = markRaw({ ... })
```

### 六、Vue2和Vue3的区别

| 特性 | Vue2 | Vue3 |
| --- | --- | --- |
| 响应式系统 | `Object.defineProperty` | `Proxy` |
| 代码组织 | Options API | Composition API |
| 源码组织 | Flow 类型系统 | TypeScript 重写 |
| 包体积 | 全量引入（22.5kb） | 按需引入（<10kb） |

**生命周期变化**

| Vue2 | Vue3 (Composition API) |
| --- | --- |
| `beforeCreate` | `setup()` |
| `created` | `setup()` |
| `beforeMount` | `onBeforeMount` |
| `mounted` | `onMounted` |
| `beforeDestroy` | `onBeforeUnmount` |
| `destroyed` | `onUnmounted` |

---

## React 基础篇

> 来源：[2025前端面试题-React基础篇](https://juejin.cn/post/7503811658198286388)

### 一、基础概念

#### 1. React核心设计思想

**组件化设计**
- 原子化构建：UI分解为独立的功能单元
- 组合模式：通过props嵌套实现组件树结构
- 隔离型：每个组件维护自身状态和样式
- 复用机制：高阶组件（HOC）与自定义Hooks实现逻辑复用

**声明式编程范式**
- 状态驱动：`UI=f(state)`的数学表达式
- 抽象渲染：开发者专注描述目标状态
- 幂等性保证：相同state必定输出相同视图

**高效更新引擎**
- 虚拟DOM层：内存中轻量级DOM表示
- Diff算法优化：启发式O(n)复杂度比较策略
- 批量更新策略：自动合并setState操作
- Fiber架构：实现可中断渲染（并发模式）

#### 2. JSX

**本质**
- JSX是React提供的语法扩展，允许在JavaScript中编写类似HTML的结构
- 会被Babel编译器转换为`React.createElement()`调用

**编译过程**
```javascript
// JSX代码
const element = <div className="title">Hello React</div>;
// 编译后
const element = React.createElement('div', { className:'title' }, 'Hello React')
```

**与HTML的关键区别**
- 属性命名：使用小驼峰命名（如`className,htmlFor,onClick`）
- 样式对象：CSS属性转为小驼峰
- 布尔值属性：属性省略值时默认为`true`
- 子元素类型：支持字符串、JSX元素、数组，`false/null/undefined`不渲染

#### 3. 生命周期方法演进

**类组件生命周期**

```javascript
class Example extends React.Component {
  // 挂载阶段
  componentDidMount() { }

  // 更新阶段
  shouldComponentUpdate(nextProps, nextState) { return true }
  componentDidUpdate(prevProps, prevState) { }

  // 卸载阶段
  componentWillUnmount() { }
}
```

**函数组件生命周期模拟**
```javascript
function Example() {
  // Mounting
  useEffect(() => {
    // componentDidMount
    return () => { /* componentWillUnmount */ };
  }, []);

  // Updating
  useEffect(() => { /* componentDidUpdate */ });

  useEffect(() => { /* 特定状态变化时执行 */ }, [count]);
}
```

#### 4. 受控组件与非受控组件

**核心定义对比**

| 组件类型 | 数据管理方式 | 控制权 |
| --- | --- | --- |
| 受控组件 (Controlled) | 表单数据由 React 组件状态驱动 | React 完全控制 |
| 非受控组件 (Uncontrolled) | 表单数据由 DOM 节点自身维护 | DOM 原生控制 |

**受控组件实现**
```javascript
function ControlledForm() {
  const [value, setValue] = useState('');

  return (
    <input
      type="text"
      value={value}
      onChange={(e) => setValue(e.target.value)}
    />
  );
}
```

**非受控组件实现**
```javascript
function UncontrolledForm() {
  const inputRef = useRef(null);

  return (
    <input
      type="text"
      defaultValue="初始值"
      ref={inputRef}
    />
  );
}
```

#### 5. 类组件和函数式组件

**核心对比**

| 维度 | 类组件 | 函数式组件 |
| --- | --- | --- |
| 定义方式 | ES6 类继承 `React.Component` | JavaScript 函数 |
| 状态管理 | `this.state` + `this.setState()` | `useState`/`useReducer` Hooks |
| 生命周期 | 完整生命周期方法 | `useEffect` 模拟生命周期 |
| 代码量 | 较冗长（需处理 `this` 绑定） | 更简洁 |

#### 6. props和state

**核心定义对比**

| 维度 | props | state |
| --- | --- | --- |
| 数据来源 | 外部传入（父组件 → 子组件） | 组件内部维护 |
| 可变性 | 只读（Immutable） | 可修改 |
| 作用范围 | 跨组件层级传递 | 组件私有 |
| 更新触发 | 父组件重新渲染时传递新 props | 调用状态更新方法触发 |

---

## React 进阶篇

> 来源：[2025前端面试题-React进阶篇](https://juejin.cn/post/7504545616841965583)

### 一、核心机制

#### 1.合成事件机制

##### 1.设计目的与核心原理

**跨浏览器一致性**
- 统一不同浏览器的事件处理接口（如`event.target`的行为）
- 修复浏览器兼容性问题（如IE事件模型差异）

**性能优化**
- **事件委托**：将事件绑定到根节点（React 17+ 为应用根DOM），而非每个子元素
- **事件池化**（`Event Pooling`）：复用事件对象，减少内存开销

**扩展能力**
- 支持自定义事件类型（如 `onDoubleClick`）
- 实现高级功能（如事件优先级调度）

##### 2.事件委托机制演进

| **React 版本** | **委托层级** | **核心变化** |
| --- | --- | --- |
| 16.x 及之前 | 所有事件委托到 `document` | 多 React 应用共存时事件可能冲突 |
| 17.x 及之后 | 委托到应用根 DOM 节点 | 隔离不同 React 版本的事件系统，避免全局污染 |

```javascript
// React 17+ 事件委托结构
const rootNode = document.getElementById('root');
ReactDOM.render(<App />, rootNode);
// 所有 React 事件监听器绑定到 rootNode 而非 document
```

##### 3.合成事件对象

**核心属性**
```javascript
interface SyntheticEvent {
  nativeEvent: Event;          // 原生事件对象
  currentTarget: DOMElement;  // 事件绑定的 React 元素
  target: DOMElement;         // 触发事件的 DOM 元素
  type: string;               // 事件类型（如 'click'）
  isDefaultPrevented(): boolean;
  isPropagationStopped(): boolean;
  persist(): void;           // 禁用事件池化
}
```

**事件池化示例**
```javascript
function handleClick(event) {
  // ❌ 错误：异步访问事件属性
  setTimeout(() => {
    console.log(event.target); // null
  }, 100);

  // ✅ 正确：保留事件引用
  event.persist();
  setTimeout(() => {
    console.log(event.target); // 正常输出
  }, 100);
}
```

##### 4.事件处理流程

**事件注册**
- React初始化时注册所有支持的事件（如`onClick, onChange`）
- 通过`EventListener`在根节点监听原生事件

**事件触发**
```
原生事件触发 → 根节点捕获事件 → React 生成 SyntheticEvent → 收集事件监听器 → 按组件树冒泡/捕获顺序执行
```

**执行顺序**
- **捕获阶段**：`父组件 onClickCapture -> 子组件 onClickCapture`
- **冒泡阶段**：`子组件 onClick -> 父组件 onClick`

##### 5.与原生事件交互

**混合使用场景**
```javascript
useEffect(() => {
  const handleNativeClick = (e) => {
    console.log('原生事件触发');
  };
  document.addEventListener('click', handleNativeClick);

  return () => {
    document.removeEventListener('click', handleNativeClick);
  };
}, []);

// React 事件处理
const handleReactClick = (e) => {
  console.log('合成事件触发');
  e.stopPropagation(); // 阻止 React 事件冒泡
};
```

**执行顺序**
```
原生事件（捕获） → 原生事件（目标） → React 事件（捕获） → React 事件（目标） → React 事件（冒泡） → 原生事件（冒泡）
```

##### 6.高频面试题

**Q1: 为什么React不直接将事件绑定在元素上？**
- 事件委托减少内存占用，动态更新组件时无需重新绑定事件

**Q2: 合成事件和原生事件的区别**
- 合成事件跨浏览器统一行为
- 原生事件直接操作DOM，无React抽象层

#### 2.组件更新触发条件与渲染优化

##### 1.组件更新触发条件

| **触发条件** | **说明** |
| --- | --- |
| **State 变化** | 组件内部 `useState`/`useReducer`/`this.setState` 更新状态 |
| **Props 变化** | 父组件重新渲染导致传入的 props 值变化 |
| **Context 更新** | 组件订阅的 Context 数据发生变更 |
| **父组件重新渲染** | 即使子组件的 props 未变化，父组件渲染仍可能导致子组件重新渲染 |
| **强制更新** | 类组件调用 `this.forceUpdate()` |
| **Hooks 依赖变化** | `useEffect`/`useMemo`/`useCallback` 的依赖数组元素变更 |

##### 2.React渲染机制核心原理

**渲染流程**
```
触发更新 -> 生成虚拟DOM -> Diff算法比较 -> 确定DOM更新范围 -> 提交到真实DOM
```

**协调策略**
- **树对比**：仅对比同层级节点，时间复杂度O(n)
- **Key值优化**：列表项使用key帮助React识别元素移动/复用

##### 3.渲染优化策略

**避免不必要的父组件渲染**
```javascript
// 父组件：将状态隔离到独立子组件
function Parent() {
  return (
    <>
      <ExpensiveChild />
      <StateContainer /> // 将易变状态抽离
    </>
  );
}
```

**组件自身渲染控制**
```javascript
// 类组件
class MyComponent extends React.PureComponent { // 自动浅比较 props/state
  shouldComponentUpdate(nextProps, nextState) {
    return !shallowEqual(this.props, nextProps);
  }
}

// 函数组件
const MemoizedComponent = React.memo(MyComponent, (prevProps, nextProps) => {
  return prevProps.id === nextProps.id; // 自定义 props 比较
});
```

**精细化Hooks使用**
```javascript
// 缓存计算结果
const expensiveValue = useMemo(() => computeValue(a, b), [a, b]);

// 稳定函数引用
const handleClick = useCallback(() => {
  // 依赖 a 但保持引用稳定
}, [a]);
```

##### 4.高频面试题

**Q1: 为什么父组件更新会导致所有子组件渲染？如何避免？**
- React默认采用"render and diff"策略，使用`React.memo/shouldComponentUpdate`阻断无效更新

**Q2: useMemo一定能提升性能吗？使用场景是什么？**
- 不一定。仅当计算开销大且稳定时使用，否则可能因依赖数组计算反增开销

**Q3: 如何优化Context引起的渲染？**
- **拆分**多个Context
- 使用`useContextSelector`按需订阅

#### 3.Hooks核心原理

##### 1.Hooks的设计目标

1. **逻辑复用**：解决类组件中高阶组件（HOC）和Render Props的嵌套地狱问题
2. **简化组件**：告别this绑定和生命周期方法的分散逻辑
3. **函数式优先**：拥抱函数式编程范式，提升代码可预测性
4. **渐进式升级**：兼容现有组件，无需重写即可逐步迁移

##### 2.核心原理

**闭包与链表存储**
- **存储结构**：Hooks数据存储在Fiber节点的`memoizedState`属性中，通过单向链表管理
- **执行顺序依赖**：Hooks调用顺序在每次渲染中必须严格一致（链表顺序不可变）
- **闭包陷阱**：每个Hooks闭包捕获当次渲染的props/state快照

```javascript
// Fiber 节点结构示意
const fiber = {
  memoizedState: {
    memoizedState: '状态值',    // useState 的状态
    next: {                   // 下一个 Hook
      memoizedState: [],      // useEffect 的依赖数组
      next: null
    }
  }
};
```

##### 3.核心Hooks原理解析

**useState**
- **存储结构**：`[state, dispatchAction]`存储在链表节点中
- **更新触发**：调用`dispatchAction`会创建更新对象，触发重新渲染
- **异步更新**：状态更新在下次渲染时生效（遵循批量更新原则）

**useEffect**
- **依赖对比**：使用浅比较（Object.is）判断依赖数组变化
- **执行时机**：在浏览器完成布局与绘制后异步执行（避免阻塞渲染）
- **清理机制**：返回的清理函数会在下次effect执行前或组件卸载前执行

**useRef**
- **跨渲染存储**：ref对象在组件生命周期内保持不变
- **直接修改**：`ref.current`的修改不会触发重新渲染

##### 4.Hooks规则的本质

**为什么必须顶层调用？**
- **链表顺序依赖**：Hooks的存储依赖调用顺序，条件语句会破坏链表结构

**为什么只能在函数组件中使用？**
- **Fiber关联**：Hooks需要绑定当前组件的Fiber节点，普通函数无此上下文

##### 5.高频面试题

**Q1: Hooks如何实现状态隔离？**
- 每个组件实例的Fiber节点维护独立的Hooks链表，相同组件不同实例互不影响

**Q2: 自定义Hook的本质是什么？**
- 将多个内置Hooks组合为可复用的逻辑单元，遵循Hooks规则

**Q3: 为什么useEffect的依赖数组是浅比较？**
- 性能优化考量，深比较成本过高。复杂对象应使用useMemo稳定引用

#### 4.常用Hooks

##### 1.基础Hooks

**useState**
- **作用**：为函数组件添加状态管理能力
```javascript
const [count, setCount] = useState(() => 0); // 惰性初始化
const increment = () => setCount(prev => prev + 1);
```

**useEffect**
- **作用**：处理副作用（数据请求、DOM操作、订阅）
- **生命周期映射**：
  - `componentDidMount -> 依赖数组为空 []`
  - `componentDidUpdate -> 指定依赖项 [dep]`
  - `componentWillUnmount -> 返回清理函数`

**useContext**
- **作用**：跨组件层级传递数据
```javascript
const ThemeContext = createContext('light');

function App() {
  return (
    <ThemeContext.Provider value="dark">
      <Toolbar />
    </ThemeContext.Provider>
  );
}

function Toolbar() {
  const theme = useContext(ThemeContext);
  return <div>{theme}</div>;
}
```

##### 2.性能优化Hooks

**useMemo**
```javascript
const expensiveValue = useMemo(() => compute(a, b), [a, b]);
```

**useCallback**
```javascript
const handleSubmit = useCallback(() => {
  submitData(name);
}, [name]);
```

**React.memo**
```javascript
const MemoComponent = React.memo(Child, (prev, next) => {
  return prev.id === next.id; // 自定义比较函数
});
```

##### 3.进阶Hooks

**useReducer**
```javascript
const initialState = { count: 0 };
function reducer(state, action) {
    switch(action.type) {
        case 'increment':
            return { count: state.count + 1 };
        default: return state;
    }
}
const [state, dispatch] = useReducer(reducer, initialState);
```

**useRef**
```javascript
const inputRef = useRef();
useEffect(() => inputRef.current.focus(), []);

// 保存上一次 props 值
const prevCount = useRef(count);
useEffect(() => {
  prevCount.current = count;
});
```

**useLayoutEffect**
- **作用**：同步执行副作用，在浏览器绘制前完成DOM修改
- **场景**：测量DOM布局、同步样式调整

##### 4.高频面试题

**Q1: useEffect和useLayoutEffect的区别？**
- useEffect异步执行（不阻塞渲染）
- useLayoutEffect同步执行（在DOM更新后，浏览器绘制前）

**Q2: useMemo和React.memo的区别？**
- useMemo缓存值
- React.memo缓存组件渲染结果

#### 5.虚拟DOM Diff算法原理

##### 1.核心设计思想

React的Diff算法基于两个核心假设，以O(n)时间复杂度完成树结构的高效比较：

1. **类型差异假设**：不同类型的元素会生成不同的树结构，直接替换整个子树
2. **Key稳定性假设**：通过key标识相同层级的元素是否可复用

##### 2.分层对比策略

React采用`逐层递归`比较，但**不会跨层级追踪节点变化**：

```javascript
旧树：<div>          新树：<section>
        <A/>               <A/>
        <B/>               <B/>
      </div>            </section>

处理逻辑：发现 div → section 类型不同，直接销毁整个 div 子树，重建 section 子树
```

##### 3.key的优化机制

| **场景** | **无 Key** | **有 Key** |
| --- | --- | --- |
| 列表项顺序变化 | 索引变化导致全部重新渲染 | 识别移动项，仅调整 DOM 顺序 |
| 列表中间插入项 | 后续项索引变化触发多节点更新 | 仅插入新节点，后续项无变化 |
| 删除中间项 | 后续项索引变化触发多节点更新 | 仅删除目标节点 |

##### 4.高频面试题

**Q1: 为什么列表必须使用key？**
- 帮助React识别元素身份，在顺序变化时高效复用DOM节点，避免以下问题：
  - 不必要的子组件重新渲染
  - 表单元素状态错乱（如输入框内容错位）

**Q2: 如何强制组件重新挂载？**
- 改变key值触发销毁/重建
```javascript
<UserProfile key={user.id} user={user} />
```

#### 6.Fiber

##### 1.Fiber的诞生背景

**旧版协调算法瓶颈**
- **递归不可中断**：同步遍历整个虚拟DOM树，长时间占用主线程
- **卡顿问题**：复杂组件树更新导致掉帧（如大型列表、动画场景）

**目标**
- 实现**增量渲染**，支持**异步可中断的更新**

##### 2.Fiber核心设计思想

1. **时间切片**：将渲染任务拆分为多个小任务（Fiber节点）
2. **优先级调度**：用户交互（如输入）优先于数据更新（如API响应）
3. **可恢复工作单元**：保存中间状态，允许暂停/恢复渲染流程

##### 3.Fiber双缓冲机制

React维护两颗Fiber树以确保更新无冲突：

1. **Current Tree**：当前已渲染的UI对应的Fiber树
2. **WorkInProgress Tree**：正在构建的新Fiber树

##### 4.优先级调度模型

| **优先级** | **对应场景** |
| --- | --- |
| ImmediatePriority | 同步任务（如 `flushSync`） |
| UserBlockingPriority | 用户交互（点击、输入） |
| NormalPriority | 数据更新、网络响应 |
| LowPriority | 过渡更新（Concurrent 模式） |
| IdlePriority | 空闲时执行的任务 |

##### 5.高频面试题

**Q1: Fiber如何实现可中断更新？**
- 通过链表结构存储Fiber节点，记录遍历进度。每次处理一个Fiber后检查剩余时间片，不足时保存当前进度，将控制权交还浏览器

**Q2: 协调阶段和提交阶段的区别？**
- 协调阶段负责计算变更（可中断），提交阶段执行DOM操作（同步不可中断）

**Q3: 为什么需要双缓冲机制？**
- 保证渲染过程中Current Tree始终完整可用，避免更新过程中出现UI不一致

### 二、状态管理

#### 1.状态管理的核心

1. **组件通信**：跨层级组件共享数据
2. **状态同步**：多个组件依赖统一数据源
3. **副作用管理**：异步操作（API请求、定时器）与状态更新的协调
4. **调试追踪**：复杂应用中状态变更的可预测性

#### 2.React内置解决方案

**组件状态（useState/useReducer）**
- **适用场景**：组件内部私有状态（如表单输入、UI切换）

```javascript
const [count, setCount] = useState(0);

// useReducer 处理复杂逻辑
const reducer = (state, action) => {
  switch (action.type) {
    case 'increment': return { count: state.count + 1 };
    default: return state;
  }
};
const [state, dispatch] = useReducer(reducer, { count: 0 });
```

**Context API**
- **适用场景**：中低频次更新的全局状态（如主题、用户身份）

```javascript
// 拆分多个Context
const ThemeContext = createContext();
const UserContext = createContext();
```

#### 3.主流状态管理库对比

| **方案** | **核心模式** | **优点** | **缺点** | **适用场景** |
| --- | --- | --- | --- | --- |
| **Redux** | 单一 Store + Flux | 严格的数据流、强大的中间件生态 | 样板代码多、学习曲线陡峭 | 大型复杂应用、需要时间旅行调试 |
| **MobX** | 响应式编程 | 代码简洁、自动追踪依赖 | 黑盒机制、过度渲染风险 | 中小型应用、快速开发 |
| **Recoil** | 原子状态 + Selector | 细粒度控制、原生支持异步 | 较新、生态不成熟 | 需要局部状态优化的场景 |
| **Zustand** | 轻量 Store | API 简单、无样板代码 | 功能相对基础 | 中小项目、替代部分 Redux 用例 |
| **Context + Hooks** | 组合式状态 | 零依赖、React 原生支持 | 性能问题、缺乏中间件 | 简单全局状态管理 |

#### 4.Redux核心原理

**三大原则**
- **单一数据源**：整个应用状态存储在一个Store中
- **只读State**：通过Action修改意图
- **纯函数Reducer**：接收旧State和Action，返回新State

**现代Redux开发（Redux Toolkit）**
```javascript
// 创建Slice
const counterSlice = createSlice({
    name: 'counter',
    initialState: 0,
    reducers: {
        increment: state => state + 1,
        decrement: state => state - 1
    }
});

// 配置Store
const store = configureStore({
    reducer: {
        counter: counterSlice.reducer
    }
});
```

#### 5.高频面试题

**Q1: Redux如何避免不必要的重新渲染？**
- 使用`reselect`创建记忆化`Selector`，避免重复计算
- 结合`React.memo`对组件进行浅比较

**Q2: MobX的响应式原理**
- 通过ES6 `Proxy`或`Object.defineProperty`追踪属性访问，自动建立观察者-被观察者关系，状态变更时触发依赖组件更新

**Q3: 如何解决Context API的性能问题？**
- 拆分多个Context隔离变化
- 使用useMemo缓存Provider的value

```javascript
const ThemeProvider = ({ children }) => {
    const [theme, setTheme] = useState('light');
    const value = useMemo(() => ({ theme, setTheme }), [theme]);
    return <ThemeContext.Provider value={value}>{children}</ThemeContext.Provider>;
}
```

---

## React 高阶篇

> 来源：[2025前端面试题-React高阶篇](https://juejin.cn/post/7504926961627054099)

### 一、React-Router

#### 1. 核心组件与API

**路由器类型**
```javascript
// BrowserRouter（使用HTML5 History API）
import { BrowserRouter } from 'react-router-dom';

// HashRouter（使用URL Hash）
<HashRouter>
  <App />
</HashRouter>
```

**路由定义**
```javascript
// v6版本
<Routes>
  <Route path="/" element={<Home />} />
  <Route path="/about" element={<About />} />
</Routes>
```

**版本差异（v5 VS v6）**

| 特性 | v5 | v6 |
| --- | --- | --- |
| 路由匹配 | 顺序匹配所有 `Route` | 优先精确匹配，使用 `Routes` |
| 嵌套路由 | 在子组件中定义 | 直接在父 `Route` 内嵌套 |
| 重定向 | `Redirect` 组件 | `Navigate` 组件 |
| 编程式导航 | `useHistory` | `useNavigate` |

#### 2. 高级技巧

**嵌套路由（v6）**
```javascript
<Route path="/users" element={<Users />}>
  <Route path=":id" element={<UserDetail />} />
</Route>

// Users 组件使用 <Outlet /> 渲染子路由
import { Outlet } from 'react-router-dom';
function Users() {
  return (
    <div>
      <h1>用户列表</h1>
      <Outlet />
    </div>
  );
}
```

**路由守卫**
```javascript
function PrivateRoute({ children }){
    const isAuthenticated = useAuth();
    return isAuthenticated ? children : <Navigate to="/login" />;
}
```

### 二、性能优化

#### 1. 关键优化策略

**组件渲染控制**
```javascript
// React.memo：缓存函数组件渲染结果
const MemoComponent = React.memo(Component, (prevProps, nextProps) => {
  return prevProps.id === nextProps.id;
});

// PureComponent：类组件自动浅比较
class OptimizedComponent extends React.PureComponent {
  // 自动实现 shouldComponentUpdate 浅比较
}
```

**高效数据管理**
```javascript
// useMemo：缓存复杂计算结果
const processedData = useMemo(() => transformData(rawData), [rawData]);

// useCallback：稳定函数引用
const handleClick = useCallback(() => {
    submitData(id);
}, [id]);
```

**列表渲染优化**
```javascript
{items.map(item => (
  <ListItem key={item.id} data={item} />
))}
```

#### 2. 并发模式优化（React18+）

```javascript
// useTransition：区分紧急/非紧急更新
const [isPending, startTransition] = useTransition();

const handleSearch = (query) => {
    startTransition(() => {
        setSearchQuery(query);
    });
};
```

### 三、高级特性

#### 1. 错误边界

```javascript
class ErrorBoundary extends React.Component {
    state = { hasError: false };

    static getDerivedStateFromError(error) {
        return { hasError: true };
    }

    componentDidCatch(error, info) {
        logErrorToService(error, info);
    }

    render() {
        if (this.state.hasError) {
            return <h1>Oops! Something went wrong.</h1>;
        }
        return this.props.children;
    }
}
```

#### 2. 高阶组件（HOC）

```javascript
function withLogging(WrappedComponent) {
    return class extends React.Component {
        componentDidMount() {
            console.log('Component is mounted:', WrappedComponent.name);
        }
        render() {
            return <WrappedComponent {...this.props} />;
        }
    }
}
```

#### 3. 服务端渲染

**Next.js实现SSR**
```javascript
export async function getServerSideProps(context) {
    const data = await fetchData(context.params.id);
    return { props: { data } };
}

function Page({ data }) {
    return <div>{data}</div>;
}
```

### 四、React18+新特性

#### 1. 并发渲染

- 时间切片：将渲染任务拆分成小块
- 优先级调度：用户交互优先于数据更新

```javascript
import { startTransition } from 'react';

const handleSearch = (query) => {
    startTransition(() => {
        setSearchQuery(query);
    });
};
```

#### 2. 自动批处理

React 18默认将多次状态更新合并为单次渲染

```javascript
// React 18：自动合并为一次渲染
const handleClick = () => {
    setCount(c => c + 1);
    setValue(v => v + 1);
};
```

#### 3. 新根API

```javascript
// React 17
ReactDOM.render(<App />, document.getElementById('root'));

// React 18
const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(<App />);
```

---

## JavaScript 中级篇

> 来源：[前端面试题-JavaScript中级篇](https://juejin.cn/post/7579154364959342611)

### 一、this 指向

#### 四大绑定规则

1. **默认绑定**: 独立函数调用，非严格模式下指向全局对象
2. **隐式绑定**: 作为对象方法调用，`this` 指向该对象
3. **显式绑定**: 通过 `.call()`, `.apply()`, `.bind()` 强制指定 `this`
4. **new 绑定**: 通过 `new` 调用构造函数，`this` 指向新创建的实例

**箭头函数的 this**
```javascript
const person = {
  name: 'Mickey',
  regularFunc: function() {
    setTimeout(function() {
      console.log(this.name); // 'window'
    }, 100);
  },
  arrowFunc: function() {
    setTimeout(() => {
      console.log(this.name); // 'Mickey'
    }, 100);
  }
};
```

### 二、原型与原型链

```javascript
function Animal(name) {
  this.name = name;
}
Animal.prototype.speak = function() { console.log('Animal speaks'); };

function Dog(name) {
  Animal.call(this, name);
}

Dog.prototype = Object.create(Animal.prototype);
Dog.prototype.constructor = Dog;

const myDog = new Dog('Buddy');
myDog.speak();
```

### 三、作用域与闭包

**闭包应用（数据封装）**
```javascript
function createCounter() {
  let count = 0;

  return {
    increment: function() {
      count++;
    },
    getValue: function() {
      return count;
    }
  };
}
```

### 四、异步编程

**事件循环示例**
```javascript
console.log('1. Script Start');

setTimeout(() => {
  console.log('5. setTimeout (Macro-task)');
}, 0);

new Promise((resolve) => {
  console.log('2. Promise Constructor');
  resolve();
}).then(() => {
  console.log('4. Promise.then (Micro-task)');
});

async function main() {
  console.log('3. async function');
  await Promise.resolve();
  console.log('6. After await (Micro-task 2)');
}
main();

// 输出: 1, 2, 3, 4, 6, 5
```

### 五、高阶函数

**函数柯里化**
```javascript
function curry(fn) {
  return function curried(...args) {
    if (args.length >= fn.length) {
      return fn.apply(this, args);
    } else {
      return function(...args2) {
        return curried.apply(this, args.concat(args2));
      }
    }
  };
}

const curriedAdd = curry((a, b, c) => a + b + c);
console.log(curriedAdd(1)(2)(3)); // 6
```

### 六、ES6+ 新特性

**解构赋值**
```javascript
const user = {
  id: 42,
  profile: {
    name: 'Mickey',
    age: 30
  }
};

const { id, profile: { name, age } } = user;
```

**展开/剩余语法**
```javascript
// 展开语法
const arr1 = ['a', 'b'];
const arr2 = [...arr1, 'c', 'd'];

// 剩余语法
function logArguments(firstArg, ...restOfArgs) {
  console.log('第一个参数:', firstArg);
  console.log('其余参数:', restOfArgs);
}
```

**Map/Set 数据结构**
```javascript
// Set 数组去重
const uniqueArray = [...new Set(arrayWithDuplicates)];

// Map 键可以是任意类型
const myMap = new Map();
myMap.set(keyObj, 'value associated with keyObj');
```

### 七、性能优化基础

**防抖**
```javascript
function debounce(func, delay) {
  let timerId;
  return function(...args) {
    const context = this;
    clearTimeout(timerId);
    timerId = setTimeout(() => {
      func.apply(context, args);
    }, delay);
  };
}
```

**节流**
```javascript
function throttle(func, limit) {
  let inThrottle = false;
  return function(...args) {
    const context = this;
    if (!inThrottle) {
      func.apply(context, args);
      inThrottle = true;
      setTimeout(() => {
        inThrottle = false;
      }, limit);
    }
  };
}
```

### 八、跨域

**CORS**
```javascript
fetch('http://api.example.com/data', {
  method: 'POST',
  headers: {
    'Content-Type': 'application/json',
  },
  body: JSON.stringify({ name: 'Mickey' })
})
```

**代理服务器**
```javascript
// 后端代理服务器代码
app.get('/api/weather', async (req, res) => {
  const { city } = req.query;
  const externalApiUrl = `https://api.weatherapi.com/v1/current.json?key=${apiKey}&q=${city}`;
  const apiResponse = await axios.get(externalApiUrl);
  res.status(apiResponse.status).json(apiResponse.data);
});
```

---

## JavaScript 基础篇

> 来源：[2025年最新前端JavaScript面试题大全](https://juejin.cn/post/7457967693481689100)

### 一、JavaScript基础

#### 1. 数据类型与类型判断

**数据类型**
- 基本类型：`string`、`number`、`boolean`、`null`、`undefined`、`symbol`、`bigint`
- 引用类型：`object`、`array`、`function`等

**类型判断**
```javascript
console.log(typeof 42); // "number"
console.log(typeof null); // "object"
console.log(Array.isArray([])); // true
console.log(Object.prototype.toString.call([])); // "[object Array]"
```

**null vs undefined**
- `undefined`：变量已声明但未赋值
- `null`：一个空对象引用，通常用于显式清空变量

#### 2. 作用域与闭包

**作用域链**
```javascript
let globalVar = "global";
function outer() {
  let outerVar = "outer";
  function inner() {
    let innerVar = "inner";
    console.log(globalVar); // "global"
    console.log(outerVar);   // "outer"
    console.log(innerVar);   // "inner"
  }
  inner();
}
```

**闭包应用**
```javascript
function createCounter() {
  let count = 0;
  return function() {
    count++;
    console.log(count);
  };
}
```

#### 3. 原型与继承

**继承方式**
1. 原型链继承
2. 构造函数继承
3. 组合继承
4. 原生式继承（Object.create）
5. 寄生式继承
6. 寄生组合式继承（最常用）

**寄生组合式继承**
```javascript
function Parent(name) {
  this.name = name;
}
Parent.prototype.sayHello = function() {
  console.log(`Hello, ${this.name}`);
};

function Child(name, age) {
  Parent.call(this, name);
  this.age = age;
}
Child.prototype = Object.create(Parent.prototype);
Child.prototype.constructor = Child;
```

### 二、ES6+新特性

#### 1. let、const与块级作用域

**let/const vs var**
- `var`存在变量提升，`let`和`const`不会
- `var`是函数作用域，`let`/`const`是块级作用域
- `const`必须初始化，且不能重新赋值

**暂时性死区（TDZ）**
```javascript
console.log(a); // ReferenceError
let a = 1;
```

#### 2. 箭头函数

**与普通函数的区别**
- 没有自己的`this`，继承自外层作用域
- 不能作为构造函数使用
- 没有`arguments`对象

```javascript
const obj = {
  value: 42,
  getValue: function() {
    return this.value;
  },
  getValueArrow: () => {
    return this.value;
  }
};
```

### 三、异步编程与事件循环

#### 1. Promise与async/await

```javascript
// Promise
const promise = new Promise((resolve, reject) => {
  setTimeout(() => {
    resolve("Success!");
  }, 1000);
});

// async/await
async function fetchData() {
  const response = await fetch("https://api.example.com/data");
  const data = await response.json();
  console.log(data);
}
```

#### 2. 事件循环

**宏任务 vs 微任务**
- 宏任务：`setTimeout`、`setInterval`、`I/O`操作
- 微任务：`Promise`回调、`MutationObserver`

```javascript
console.log("Start");
setTimeout(() => console.log("Timeout"), 0);
Promise.resolve().then(() => console.log("Promise"));
console.log("End");
// 输出: Start -> End -> Promise -> Timeout
```

### 四、性能优化

#### 防抖与节流

```javascript
// 防抖
function debounce(func, wait) {
  let timeout;
  return function(...args) {
    clearTimeout(timeout);
    timeout = setTimeout(() => func.apply(this, args), wait);
  };
}

// 节流
function throttle(func, wait) {
  let lastTime = 0;
  return function(...args) {
    const now = Date.now();
    if (now - lastTime >= wait) {
      func.apply(this, args);
      lastTime = now;
    }
  };
}
```

---

## 附录：高频面试题速查

### TypeScript

**Q1: any和unknown的主要区别是什么？**
> A：`any`禁用所有类型检查；`unknown`要求在使用前显式断言或类型检查

**Q2: void和undefined在函数返回值中的区别？**
> A：`void`表示函数没有有效返回值，`undefined`要求必须显式返回`undefined`值

### Vue3

**Q1: keep-alive实现原理**
> A：通过Map缓存组件vnode实例，渲染时直接从缓存中取；DOM从文档流移除/重新插入

**Q2: Vue3比Vue2快在哪里？**
> A：Proxy替代defineProperty；Patch Flags标记动态节点；Tree Shaking支持更佳

### React

**Q1: 为什么componentWillXXX系列方法被标记为UNSAFE？**
> A：异步渲染模式下可能被多次调用；副作用操作可能导致渲染不一致

**Q2: useEffect与生命周期方法的对应关系？**
> A：`useEffect(fn, []) -> componentDidMount + componentWillUnmount`

### JavaScript

**Q1: 事件循环的执行顺序？**
> A：同步代码 → 清空微任务队列 → UI渲染(如需要) → 取一个宏任务 → 循环

**Q2: var、let、const的区别？**
> A：var有变量提升和函数作用域；let/const有块级作用域和暂时性死区；const不可重新赋值

---

**说明**：本文档内容整理自掘金【2025前端面试题】系列，仅供学习参考。
